# 多语言切换 TODO 列表

- [x] 在所有页面中统一语言选择器的初始化方式，依赖 `assets/js/i18n/registry.js` 的 `populateLanguageSelect` 输出语言标签与备用内容提示，避免手写 `<option>` 造成的不一致。
- [x] 新增共享的语言选择器增强工具，集中处理选择框的事件绑定、方向属性与当前语言标记，供首页、友链页与研究文稿页复用。
- [x] 为每个页面的语言选择器增加 `data-language-toggle` 标记，并在脚本中通过该标记自动识别组件，保持结构一致性与可扩展性。
- [x] 在语言切换时同步更新选择器的 `lang` 与 `dir` 属性，确保诸如阿拉伯语等右到左语言在界面上正确呈现。

